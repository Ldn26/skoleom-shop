/**
 * Alias rétro-compatible.
 * Le rendu « boutiques audiovisuelles » est une variante du méga-menu
 * unifié (`Navbar` avec variant="stores"). Ce fichier ne fait que
 * ré-exporter le composant pour éviter de casser un éventuel import existant.
 */
export { default } from './Navbar';
